# Mock E‑Com 

First you have to run Backend and then run the Frontend on http://localhost:3000.

## Run backend
cd backend
npm install
npm start


## Run frontend
cd frontend
npm install
npm start


Frontend runs at http://localhost:3000 and expects backend at http://localhost:5000 by default.
